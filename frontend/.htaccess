<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /~cakir4/CycleD/frontend/
    
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]
    
    RewriteRule ^ index.html [L]
</IfModule>
